### Javascript API for [dormouse](http://dormou.se)

- Connects to dormouse server
- queues tasks
- receives responses
- browser friendly

### First steps

- set `$dm.server` and `$dm.api_key` to access dormouse
- use the `$dm` object

